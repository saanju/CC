pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building Docker image...'
               
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
               
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying Docker image...'
                
            }
        }

        // Add other stages as needed
    }
}
